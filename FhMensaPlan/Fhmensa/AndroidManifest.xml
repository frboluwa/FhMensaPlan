<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="de.fhswf.android"
    android:versionCode="1"
    android:versionName="1.0" >
    
    <uses-permission
        android:name="android.permission.INTERNET" />

    <uses-sdk android:minSdkVersion="8" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" >
        <activity
            android:name=".FhmensaActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HagenActivity" android:label="@string/hagen"></activity>
        <activity android:name=".IserlohnActivity" android:label="@string/iserlohn"></activity>
        <activity android:name=".MeschedeActivity" android:label="@string/meschede"></activity>
        <activity android:name=".SoestActivity" android:label="@string/soest"></activity>
        <activity android:name=".LuedenscheidActivity" android:label="@string/luedenscheid"></activity>
    </application>

</manifest>